server:
  port: 8086

spring:
  application:
    name: identity-service

user-service:
  url: ${USER_SERVICE_URL:http://localhost:8085}

jwt:
  secret: ${JWT_SECRET:change-me-super-secret-jwt-signing-key-256-bits-minimum-length-ok}
  expiration-minutes: ${JWT_EXPIRATION_MINUTES:60}
